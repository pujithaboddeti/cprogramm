//write a program to print sum of all the array elements

#include <stdio.h>
main()
{
  int i,n,x;
  scanf("%d",&n);
  int a[n];
   for(i=0;i<n;i++)
   {
       scanf("%d",&a[i]);
       x+=a[i];
   }
   printf("the total:%d",x);
}
